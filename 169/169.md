# 169. Majority Elelemnt (Easy)

my first solution was not pretty IMO. I did not like that I use 4 variables.
1 for current num and current count, one for max count and max num.

Simple divide and conquer sort then count.

I think a cooler solution would be storying them in some type of hashmap.

unique_elements = set(nums)
        majority = 0
        for i in unique_elements:
            if nums.count(i) > (len(nums)/2):
                majority = i
        return majority

This was the best solution.
I this solution was good because it organizes by the unique elements
then utilizes count of one type of element to see which is greator then the condition, once it finds
the majority it sets it and returns.
